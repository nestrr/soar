// This ensures that things work correctly for the "children" implicit slot. This will be the fallback for children if next.js cannot recover the active state of the parent page.
export default function Page() {
  return <></>;
}
